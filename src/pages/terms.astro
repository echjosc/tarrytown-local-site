---
import Base from "@/layouts/Base.astro";
import PageHero from "@/components/ui/PageHero.astro";
import { createReader } from '@keystatic/core/reader';
import keystaticConfig from '../../keystatic.config';

const reader = createReader(process.cwd(), keystaticConfig);
const termsPage = await reader.singletons.termsPage.read();

const paragraphs = (termsPage?.body || '').split('\n\n').filter(Boolean);
---

<Base
	title={termsPage?.seoTitle || "Terms of Service"}
	description="Tarrytown Local's terms of service."
>
	<!-- Hero -->
	<PageHero
		title={termsPage?.heroTitle || "Terms of Service"}
		subtitle={termsPage?.lastUpdated ? `Last updated: ${termsPage.lastUpdated}` : undefined}
	/>

	<!-- Content -->
	<section class="legal-body section">
		<div class="container">
			{paragraphs.map((p) => <p class="legal-para">{p}</p>)}
		</div>
	</section>
</Base>

<style>
	.container {
		max-width: 720px;
	}

	.legal-para {
		font-size: 0.95rem;
		color: var(--color-text-muted);
		line-height: 1.85;
		margin-bottom: 1.5rem;
	}

	.legal-para:last-child {
		margin-bottom: 0;
	}
</style>
